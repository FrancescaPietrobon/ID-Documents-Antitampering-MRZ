#include "EuclideanDistance.hpp"

EuclideanDistance::EuclideanDistance(float e):
    eps(e){};

std::vector<Fields> EuclideanDistance::associateCharacters(const Characters *characters, const size_t charactersSize)
{
    std::vector<Fields> fields;

    return fields;
}